name: Build and push for both amd64 and arm64

on:
  workflow_dispatch:
    inputs:
      push_to_ghcr:
        description: Also push images to GitHub Container Registry (ghcr.io)
        type: boolean
        required: false
        default: false

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    env:
      PUSH_TO_GHCR: ${{ inputs.push_to_ghcr }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Normalize image owner (lowercase for GHCR)
        shell: bash
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_ENV"

      # -------------------------------
      # Setup emulation + builder
      # -------------------------------
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # -------------------------------
      # Login to Docker Hub
      # -------------------------------
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # -------------------------------
      # Login to GitHub Container Registry (GHCR)
      # -------------------------------
      - name: Login to GHCR
        if: ${{ env.PUSH_TO_GHCR == 'true' }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          # Prefer a PAT for GHCR when publishing a package name that doesn't match the repo name (e.g. "caddy"),
          # otherwise pushes may fail with 403 depending on package ownership/settings.
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN || secrets.MY_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}

      # -------------------------------
      # Build and Push for amd64
      # -------------------------------
      - name: Build and push (amd64)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/caddy:amd64
            ${{ secrets.DOCKERHUB_USERNAME }}/caddy:latest
            ${{ format('ghcr.io/{0}/caddy:amd64', env.OWNER_LC) }}
            ${{ format('ghcr.io/{0}/caddy:latest', env.OWNER_LC) }}
          cache-from: type=gha,scope=caddy-amd64
          cache-to: type=gha,mode=max,scope=caddy-amd64
        if: ${{ env.PUSH_TO_GHCR == 'true' }}

      - name: Build and push (amd64)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/caddy:amd64
            ${{ secrets.DOCKERHUB_USERNAME }}/caddy:latest
          cache-from: type=gha,scope=caddy-amd64
          cache-to: type=gha,mode=max,scope=caddy-amd64
        if: ${{ env.PUSH_TO_GHCR != 'true' }}

      # -------------------------------
      # Build and Push for arm64
      # -------------------------------
      - name: Build and push (arm64)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/arm64
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/caddy:arm64
            ${{ secrets.DOCKERHUB_USERNAME }}/caddy:latest
            ${{ format('ghcr.io/{0}/caddy:arm64', env.OWNER_LC) }}
            ${{ format('ghcr.io/{0}/caddy:latest', env.OWNER_LC) }}
          cache-from: type=gha,scope=caddy-arm64
          cache-to: type=gha,mode=max,scope=caddy-arm64
        if: ${{ env.PUSH_TO_GHCR == 'true' }}

      - name: Build and push (arm64)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/arm64
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/caddy:arm64
            ${{ secrets.DOCKERHUB_USERNAME }}/caddy:latest
          cache-from: type=gha,scope=caddy-arm64
          cache-to: type=gha,mode=max,scope=caddy-arm64
        if: ${{ env.PUSH_TO_GHCR != 'true' }}

      # -------------------------------
      # Create multi-arch manifest for "latest" tag
      # -------------------------------
      - name: Create and push multi-arch manifest (Docker Hub)
        run: |
          docker buildx imagetools create \
            -t ${{ secrets.DOCKERHUB_USERNAME }}/caddy:latest \
            ${{ secrets.DOCKERHUB_USERNAME }}/caddy:amd64 \
            ${{ secrets.DOCKERHUB_USERNAME }}/caddy:arm64

      - name: Create and push multi-arch manifest (GHCR)
        if: ${{ env.PUSH_TO_GHCR == 'true' }}
        run: |
          docker buildx imagetools create \
            -t ghcr.io/${{ env.OWNER_LC }}/caddy:latest \
            ghcr.io/${{ env.OWNER_LC }}/caddy:amd64 \
            ghcr.io/${{ env.OWNER_LC }}/caddy:arm64